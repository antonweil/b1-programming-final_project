# b1-programming-final_project
dependencies:
    fastapi
    typing
    uvicorn
    pydantic

main Github branch is the base branch which only contains required features, other branches may contain expansions (like database storage, frontend, backups)